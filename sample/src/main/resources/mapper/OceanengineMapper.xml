<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.classmatechen.sample.mapper.OceanengineMapper">

    <select id="select" resultType="org.classmatechen.sample.po.Oceanengine">
        select app_id, secret, auth_code from oceanengine where app_id = #{ appId }
    </select>

    <select id="list" resultType="org.classmatechen.sample.po.Oceanengine">
        select app_id, secret, auth_code from oceanengine where `delete` = 0
    </select>
</mapper>
