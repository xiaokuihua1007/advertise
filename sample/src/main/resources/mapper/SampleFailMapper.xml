<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.classmatechen.sample.mapper.SampleFailMapper">

    <insert id="insert">
        insert into sample_fail
        (sampler, param, param_class, context, context_class, occur_time, error, finish) value
        (#{ sampler }, #{ param }, #{ paramClass }, #{ context }, #{ contextClass }, #{ occurTime }, #{ item.error }, 0)
    </insert>
    
    <insert id="batchInsert">
        insert into sample_fail (sampler, param, param_class, context, context_class, occur_time, error, finish) values
            <foreach collection="list" item="item" separator=",">
                (#{ item.sampler }, #{ item.param }, #{ item.paramClass }, #{ item.context }, #{ item.contextClass }, #{ item.occurTime }, #{ item.error }, 0)
            </foreach>
    </insert>
    
    <select id="list" resultType="org.classmatechen.sample.po.SampleFail">
        select * from sample_fail where 1 = 1
        <if test="null == isDelete">
            and finish = 0
        </if>
        <if test="null != isDelete and false == isDelete">
            and finish = 0
        </if>
        <if test="null != isDelete and true == isDelete">
            and finish = 1
        </if>
        <if test="null != startOccurTime and null != endOccurTime">
            and occur_time between #{ startOccurTime } and #{ endOccurTime }
        </if>
        <if test="null != sampler and '' != sampler">
            and sampler = #{ sampler }
        </if>
    </select>

    <!-- <update id="deleteById">
        update sample_fail set finish = 1 where id = #{ id }
    </update> -->

    <delete id="deleteById">
        delete from sample_fail where id in (
            <foreach collection="list" item="item" separator=",">
                #{ item }
            </foreach>
        )
    </delete>

    <select id="groupBySampler" resultType="org.classmatechen.sample.po.SampleFail">
        select sampler from sample_fail where 1 = 1 and finish = 0 group by sampler
    </select>
</mapper>
