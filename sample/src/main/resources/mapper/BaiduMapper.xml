<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.classmatechen.sample.mapper.BaiduMapper">

    <select id="select" resultType="org.classmatechen.sample.po.Baidu">
        select user_id, app_id, auth_code, secret_key from baidu where user_id = #{ userId }
    </select>

    <select id="list" resultType="org.classmatechen.sample.po.Baidu">
        select user_id, app_id, auth_code, secret_key from baidu
    </select>
</mapper>
